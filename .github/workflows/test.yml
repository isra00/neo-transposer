name: Test Neo-Transposer
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 7.4
          extensions: curl
          tools: composer, phpunit

      - name: Install dependencies
        run: composer install

      - name: Run PHPUnit
        run: phpunit

      - name: Turn on Ubuntu MySQL (SUDO)
        run: |
          sudo systemctl enable mysql.service
          sudo systemctl start mysql.service

      #- uses: mirromutth/mysql-action@v1.1
        #with:
          #mysql version: 5.7
          #mysql user: test
          #mysql password: test
          #host port: 3800 
          #container port: 3307 

      - name: Load MySQL data
        run: |
          mysql -uroot -proot -e 'SHOW VARIABLES LIKE "%version%";'
#          CREATE DATABASE transposer COLLATE utf8_general_ci'
#          mysql -utest -ptest --port=3800 transposer < song_data.sql
#          php tests/testAllTranspositions.php
